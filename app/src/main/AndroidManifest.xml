<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.poly.printcode">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".function.EditProductActivity"></activity>
        <activity android:name=".function.ProductDetailActivity" />
        <activity android:name=".function.AllProductActivity" />
        <activity android:name=".function.AddProductActivity" />
        <activity android:name=".function.AllfriendActivity" />
        <activity android:name=".function.CheckUpdate" />
        <activity android:name=".function.UpdateActivity" />
        <activity android:name=".function.UserActivity" />
        <activity android:name=".function.MainSignUp" />
        <activity android:name=".function.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>